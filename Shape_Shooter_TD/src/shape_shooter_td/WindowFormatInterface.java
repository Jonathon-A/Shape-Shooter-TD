package shape_shooter_td;

public interface WindowFormatInterface {
    //Plays start of transition, adds the new panel and removes the previous before end the transition
    public void StartSetup(final int TransitionType);
}
